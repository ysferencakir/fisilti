[build]
builder = "dockerfile"

[deploy]
startCommand = "python manage.py migrate && gunicorn fisilti.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/users/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
